import React from 'react';
import './Banner.css';

const Banner = () => (
  <div className="banner text-center d-flex align-items-center justify-content-center">
    <p className="m-0">Click each image without clicking the same one twice, or the Doge will be dissapointed.</p>
  </div>
);

export default Banner;
